# Learning Dapper: Usage, CRUD, and EF Core Comparison

A simple .NET console project that explains **Dapper** usage and compares its performance with  
**Entity Framework Core (EF Core)** using **BenchmarkDotNet**.

---

## 🚀 Overview

This repository demonstrates the differences between:

- **Dapper** — lightweight and fast data access library  
- **Entity Framework Core (EF Core)** — full-featured ORM for .NET  
- **BenchmarkDotNet** — used to measure execution performance between approaches  

---

## 🛠 Requirements

Make sure you have:

- ✔ **.NET 8 SDK**
- ✔ **SQL Server** with **Northwind** database
- ✔ **Visual Studio 2022 or later** (required for `.slnx`)
